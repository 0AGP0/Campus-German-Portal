node_modules
dist

# Next.js derleme çıktısı (VPS’te build sonrası git pull çakışmasın diye repoda tutulmamalı)
.next/
out/
.DS_Store
*.local
.env
.env.local
!.env.example
prisma/*.sqlite
prisma/*.sqlite-journal

# Webhook yüklemeleri (pasaport / imza)
uploads/
